# 単一パッケージだが pnpm v10+ の設定ファイルとして利用する

# postinstall でネイティブバイナリを取得するパッケージのビルドを許可
allowBuilds:
  '@typescript/native-preview': true

# 公開直後（3日 = 4320分）のバージョンは取り込まない（旧 bunfig minimumReleaseAge 相当）
minimumReleaseAge: 4320
